using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Fungus;

public class SceneManagerScript : MonoBehaviour {
    // Start is called before the first frame update

    private Flowchart gameManager;
    void Start() {
       // gameManager = GameObject.FindWithTag("GameManager").GetComponent<Flowchart>();
       // gameManager.ExecuteBlock("SceneLoaded");
    }

    // Update is called once per frame
    void Update() {

    }
}
